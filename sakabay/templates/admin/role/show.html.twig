{% extends 'base.html.twig' %}

{% block title %}{{ 'role_title_show_page'|trans }}{% endblock %}

{% block body %}
    <show-role
    :role-id="{{ roleId }}"
    :can-edit="{{ canEdit|json_encode }}"
    url-precedente="{{ urlPrecedente }}"
    ></show-role>
{% endblock %}
